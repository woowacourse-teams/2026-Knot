/**
 * 탐색(채팅) 화면으로 이동할 때 함께 넘기는 값.
 *
 * 하단 독에서 질문을 적어 보내면 탐색 화면으로 옮겨 가면서 그 질문을 들고 가요.
 * 화면이 바뀌는 것과 질문이 전달되는 것이 한 번에 일어나야 해서 주소가 아니라 히스토리 state로 넘겨요.
 */
export interface ChatNavigationState {
  /** 도착하자마자 보낼 질문 */
  question?: string;
}
